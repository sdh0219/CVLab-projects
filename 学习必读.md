# 学习必读 · 如何用好这套项目指南（全体学员先读这一页）

> 这套仓库有 11 个项目，每个项目一份《复现指南.md》。**指南写得很细，但再细的指南也代替不了正确的使用方法**。花 10 分钟读完这一页，你会少走一半的弯路。

---

## 一、指南的四部分怎么用

每份指南都分四部分，对应你的三个阶段：

| 阶段 | 读哪部分 | 怎么读 |
|---|---|---|
| 动手前 | 第一部分 · 项目导览 | 通读一遍，看懂"这是什么、长什么样"。**看不懂的技术名词先混个脸熟**，导览里的人话解释后面还会反复出现 |
| 动手时 | 第二部分 · 复现手册 | **一步一步对着做，每做完一步就找"成功标志"**。看到了 = 对了；没看到 = 停下来排查，别硬往下做 |
| 跑通后 | 第三部分 · 理解手册 | 这才是"学到技术"的部分。对照源码读概念讲解，做完"动手改一改"的练习 |
| 随时 | 第四部分 · 附录 | 报错查"速查表"，名词查"术语表"，想深入看"扩展阅读" |

## 二、四个必会的基本功

### 1. 对"成功标志"

指南每一步都写明了成功时**终端会打印什么、文件夹会出现什么**。养成习惯：每执行一条命令，先找成功标志，再执行下一条。**连续执行五六条命令再回头看的，必然返工。**

### 2. 查版本

很多报错的根因是版本不对。三个常用查询命令（在 cmd 里输）：

```
python --version      ← Python 版本（多数项目要求 3.10~3.12）
pip list              ← 已安装的包和版本
node --version        ← Node.js 版本（仅网页类项目需要）
```

对照指南附录 C 的"验证记录"里列出的版本——**和你的一致，问题就不在环境**。

### 3. 读日志（报错只看最后 5 行）

程序报错时终端会刷一大片红字，**有效信息几乎都在最后 5 行**。读懂一句万能句式：

```
XXXError: 人类可读的原因描述
```

`XXXError` 是错误类型（FileNotFoundError=找不到文件、ModuleNotFoundError=缺包、PermissionError=权限/占用），冒号后面是具体原因。**把最后 5 行拿去对照指南的"报错速查表"**，90% 的问题 1 分钟内解决。

### 4. 改动前备份

做"动手改一改"练习或自己折腾代码之前，先复制一份整个项目文件夹（改名加 `_backup` 后缀）。改崩了直接删掉重来，**永远有一份能跑的底版**。

## 三、卡住了怎么求助（提问的正确姿势）

按下面的模板提问，别人 30 秒就能帮你定位；乱截图问"为什么不行"，神仙也帮不了：

1. **我在做哪一步**：指南 2.4 节，运行 `python xxx.py`
2. **终端最后 5 行报错**：原样复制粘贴（不要截图，截图没法复制关键字）
3. **我已经试过什么**：比如"按速查表第 2 条重装了依赖，还是报错"
4. 环境：Windows 版本 + `python --version` 结果

提交渠道：**在 GitHub 仓库提 Issue**（https://github.com/sdh0219/CVLab-projects/issues），标题写"项目编号+卡住的步骤"，例如"[QD05] 2.6 装依赖报错"。Issue 会沉淀下来，后面同学的同类问题直接搜 Issue 就有答案——**你的踩坑就是下一届的速查表**。

## 四、三条学习心法

1. **报错是教材不是事故**：每解决一个报错，你就多懂一个原理。怕报错的人学不会编程。
2. **先跑通，再深究**：卡在某个概念上就先跳过，跑通整条流程后再回头读理解手册——全局感会帮你理解细节。
3. **改一次，看一次**：做练习时，改一个参数 → 运行 → 对比变化 → 记录结论。这个循环比读十篇教程都有效。

## 五、怎么下载项目

全部项目放在一个 GitHub 仓库里：**https://github.com/sdh0219/CVLab-projects**

**方式 A · 网页下载 ZIP（零基础推荐，不用装任何东西）**：

1. 浏览器打开上面的地址；
2. 点页面上方的绿色 **`<> Code`** 按钮；
3. 点 **Download ZIP**，等待下载完成（仓库包含全部 11 个项目，文件较大请耐心）；
4. 右键压缩包 → 全部解压，得到 `CVLab-projects-main` 文件夹——**你要做的项目就在里面，进入对应项目文件夹**，其余 10 个用不到可以不管。

**方式 B · git clone（如果以后想学 Git，推荐尝试）**：

```
git clone https://github.com/sdh0219/CVLab-projects.git
```

下载得到的内容和 ZIP 完全一样。

**方式 C · 离线兜底**：GitHub 在国内偶尔打不开或下载很慢。遇到这种情况直接**找老师要离线压缩包**，不要在网络上耗时间。

> 下载好后先做一件事：打开项目文件夹里的《复现指南.md》，从第一部分读起。

## 六、各项目难度与建议顺序

| 顺序 | 项目 | 类型 | 上手难度 | 建议理由 |
|---|---|---|---|---|
| 1 | QD14 救援路径规划 | 纯 Python 算法 | ⭐ 最易 | 依赖只有 1 个包，5 秒出结果，最适合建立信心 |
| 2 | QD03 雨量计数据清洗 | 数据处理 | ⭐⭐ | 数据自动生成，30 秒跑通，学数据思维 |
| 3 | QD09 地震预警系统 | Web 入门 | ⭐⭐ | 第一次体验"起一个网站" |
| 4 | QD17 恢复周期预测 | 机器学习 | ⭐⭐ | 表格数据的完整建模流程 |
| 5 | QD05 洪水水位监测 | 深度学习 | ⭐⭐⭐ | 第一次训练神经网络（耗时较长） |
| 6 | QD01 数据孤岛融合 | 数据工程 | ⭐⭐⭐ | 多源数据清洗 + Streamlit 看板 |
| 7 | QD18 泥石流零死亡 | GIS 分析 | ⭐⭐⭐ | 栅格分析 + 加密授权机制 |
| 8 | QD13 物资分配优化 | 优化算法 | ⭐⭐⭐ | 遗传算法 + 前后端 |
| 9 | QD15 AI 应急指挥舱 | 前后端分离 | ⭐⭐⭐⭐ | FastAPI+Vue3 完整工程 |
| 10 | QD02 知识图谱 | GraphRAG | ⭐⭐⭐⭐ | 前沿方向，概念较多 |
| 11 | QD12 求救信息提取 | NLP 深度学习 | ⭐⭐⭐⭐⭐ | 需下载模型+训练等待，适合压轴 |

> 顺序不是死的：如果你对某个方向特别感兴趣，直接跳过去也没问题——每个项目都是自包含的。

---

**现在开始**：从上面选一个项目，进入它的文件夹，打开《复现指南.md》，从第一部分读起。祝复现顺利！
